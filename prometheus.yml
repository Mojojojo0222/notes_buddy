global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'notes-buddy'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['notes-buddy:9098']   # Docker service name resolves automatically
