global:
  scrape_interval: 15s

rule_files:
  - 'prometheus-alerts.yml'

scrape_configs:
  - job_name: 'notes-buddy'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['notes-buddy:9098']
